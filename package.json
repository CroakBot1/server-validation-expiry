{
  "name": "uuid-server-login",
  "version": "1.0.0",
  "description": "Server-based UUID login with 1 month expiration and keep-alive ping",
  "main": "server.cjs",
  "scripts": {
    "start": "node server.cjs",
    "build": "echo 'No build needed for server-only setup'",
    "dev": "nodemon server.cjs"
  },
  "dependencies": {
    "express": "^4.18.2",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
