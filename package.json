{
  "name": "uuid-server-login",
  "version": "1.0.0",
  "description": "Server-based UUID login with 1 month expiration",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "echo 'No build needed for server-only setup'"
  },
  "dependencies": {
    "express": "^4.18.2",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5"
  }
}
